<hr><hr>
    <h3><i><center>The Box Shop</center></h3>
<hr>
<small><small> Restock items in Orange, Fast sell items in Green </i></small></small>
<br>
<!-- Looking at the data in the object : debugging -->
<%#= @smoothie_items.inspect %>

<ul>
  <% @smoothie_items.each do | s_item | %>
<!-- Not sure why the second h3 if tag overwrites the prevous one : lookup -->
  <h3 class="<%= "grn" if s_item.qty >= 14 %>">
  <h3 class="<%= "org" if s_item.qty <= 3 %>">
<!-- Not sure why the second h3 if tag overwrites the prevous one : lookup -->
    <li><%=s_item.name %></li></h3>
    <i> : Size <%= s_item.size %>
      : Units <%= s_item.units %>
      : Qty <%= s_item.qty %>
      : Type <%= s_item.descrip %>
    </i><hr>
  <% end %>
</ul>
<hr>
<ul>
  <li> <a href="/"> Return </a></li>
</ul>
<hr><hr>
